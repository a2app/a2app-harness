[package]
name = "makepad-host"
version = "0.1.0"
edition = "2024"

[dependencies]
autosurgeon = "0.8"
async-trait = "0.1"
reqwest = { version = "0.12", features = ["json"] }
tokio = { version = "1", features = ["full"] }
shared = { path = "../shared" }
