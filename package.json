{
  "name": "a2app-harness",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "extension"
  ],
  "scripts": {
    "build:extension": "npm --workspace extension run build",
    "typecheck:extension": "npm --workspace extension run typecheck"
  }
}
